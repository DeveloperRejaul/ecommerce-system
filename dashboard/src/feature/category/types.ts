import { IShopTypes } from "../shop/types";

export interface ICategoryType {
    name: string;
    shopId: IShopTypes;
    avatar: string;
    _id: string;
    createdAt: string;
    updatedAt: string;
    __v: string;
}
