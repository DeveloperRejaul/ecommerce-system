
const {Router} = require('express');
const router = Router();
module.exports = () => {

	router.get('/coupon', ()=>{});
	router.post('/coupon', ()=>{});
	router.put('/coupon', ()=>{});
	router.delete('/coupon', ()=>{});

	return router; 

};